// grab charge button DOM elements
const charge1 = document.getElementById("c1");
const charge2 = document.getElementById("c2");

// grab fps button DOM elements
const slowBtn = document.getElementById("slow-btn");
const fastBtn = document.getElementById("fast-btn");
const fastestBtn = document.getElementById("fastest-btn");

// grab toggleMode arrows/lines button
const arrowMode = document.getElementById("arrowMode");
const lineMode = document.getElementById("lineMode");
